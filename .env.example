# Скопируйте в .env и настройте
PORT=8080
# API_KEY=your-secret-key
DB_PATH=/app/data/openstat.db
STATUS_PATH=/var/log/openvpn/status.log
INTERVAL=60s
RETENTION=1000
# Ресурсы контейнера
MEMORY_LIMIT=256M
MEMORY_RESERVATION=64M
CPU_LIMIT=0.5
# Разрешённые пути для /collect (через запятую, защита от path traversal)
# ALLOWED_PATHS=/var/log/openvpn
# Путь к status-файлу на хосте (для volume)
# OPENVPN_STATUS_DIR=./data/openvpn-status
# OPENVPN_STATUS_DIR=/var/log/openvpn